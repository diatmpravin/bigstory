<h1>Listing signups</h1>

<table>
  <tr>
    <th>First name</th>
    <th>Last name</th>
    <th>Email</th>
    <th>Password</th>
    <th>Re password</th>
  </tr>

<% @signups.each do |signup| %>
  <tr>
    <td><%=h signup.first_name %></td>
    <td><%=h signup.last_name %></td>
    <td><%=h signup.email %></td>
    <td><%=h signup.password %></td>
    <td><%=h signup.re_password %></td>
    <td><%= link_to 'Show', signup %></td>
    <td><%= link_to 'Edit', edit_signup_path(signup) %></td>
    <td><%= link_to 'Destroy', signup, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New signup', new_signup_path %>